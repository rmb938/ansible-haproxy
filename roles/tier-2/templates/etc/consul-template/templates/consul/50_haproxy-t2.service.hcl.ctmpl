# {{ ansible_managed }}
# Consul Template Managed - Consul

service {
  name = "haproxy-t2"
  id   = "haproxy-t2"
  port = 443

  checks = [
    {
      http = "http://localhost"
      interval = "5s"
    }
  ]
}
